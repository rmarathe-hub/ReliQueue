[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    reliability: core retry, DLQ, lease, cancel, and event-timeline scenarios (test_reliability.py)
    slow: high-volume concurrency stress tests; exclude with pytest -m "not slow" for CI
